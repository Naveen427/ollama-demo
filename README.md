# ollama-demo

* Install ollama
* Run gemma3 in your local `ollama run gemma3`
* Start the application and hit the `/chat` endpoint with your question using the request param `message`
* e.g, `client.http`